version: 2

updates:
  # Go module dependencies (backend/)
  - package-ecosystem: gomod
    directory: /backend
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 10
    labels:
      - dependencies
      - go
    commit-message:
      prefix: build
      include: scope
    groups:
      # Every Go group is restricted to minor/patch. Unlike npm, a Go major
      # changes the import path (/v2, /v3), so it always needs source changes
      # -- bundling one into a group PR would block that group's harmless
      # updates behind a breaking change. Majors therefore arrive as
      # individual PRs. (The npm groups below deliberately differ: peer-coupled
      # JS packages must install in lockstep, majors included.)
      aws-sdk:
        patterns:
          - "github.com/aws/*"
        update-types:
          - minor
          - patch
      gorm:
        patterns:
          - "gorm.io/*"
        update-types:
          - minor
          - patch
      opennsw:
        patterns:
          - "github.com/OpenNSW/*"
        update-types:
          - minor
          - patch
      # Long tail: bundle minor/patch into one PR.
      minor-and-patch:
        update-types:
          - minor
          - patch

  # Frontend (pnpm)
  - package-ecosystem: npm
    directory: /frontend
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 10
    labels:
      - dependencies
      - javascript
    commit-message:
      prefix: build
      include: scope
    groups:
      # Peer-coupled packages — must bump in lockstep, even on majors.
      react-ecosystem:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react"
          - "@types/react-dom"
      vite-ecosystem:
        patterns:
          - "vite"
          - "@vitejs/*"
          - "@tailwindcss/*"
          - "tailwindcss"
      eslint-ecosystem:
        patterns:
          - "eslint*"
          - "@eslint/*"
          - "typescript-eslint"
          - "globals"
      # Long tail (typescript, radix-ui, zod, i18next, jsonforms, …):
      # one PR for minor/patch. Majors of ungrouped packages still arrive
      # as individual PRs for review.
      minor-and-patch:
        update-types:
          - minor
          - patch

  # GitHub Actions
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 10
    labels:
      - dependencies
      - github_actions
    commit-message:
      prefix: ci
      include: scope
    groups:
      actions:
        patterns:
          - "*"
        update-types:
          - minor
          - patch

  # Docker base images
  #   / -> node:22-alpine3.24, golang:1.27-alpine3.24, alpine:3.24
  - package-ecosystem: docker
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 10
    labels:
      - dependencies
      - docker
    commit-message:
      prefix: build
      include: scope
    ignore:
      # The golang builder tag must move together with the `go` directive in
      # backend/go.mod, and the gomod ecosystem never bumps that directive —
      # so letting docker bump `golang` alone would desync the two one-sidedly.
      # Bump both by hand, in one commit. See the FROM line in the Dockerfile.
      - dependency-name: "golang"
    groups:
      # Bundle minor/patch base-image bumps; major base-image jumps
      # (e.g. node 22 -> 24) still arrive individually for review.
      minor-and-patch:
        update-types:
          - minor
          - patch
