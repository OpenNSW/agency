name: Backend CI

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'backend/**'
      - '.github/workflows/backend-ci.yml'

jobs:
  quality-gate:
    name: Quality Gate
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: './backend/go.mod'
          cache-dependency-path: backend/go.sum
          cache: true

      - name: Verify go.mod
        run: |
          go mod tidy
          git diff --exit-code go.mod go.sum

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.13.2
          # Build golangci-lint from source with the Go toolchain set up above
          # (from go.mod). The prebuilt release binaries are compiled with an
          # older Go and their embedded type-checker panics on our go 1.27
          # module ("package requires newer Go version go1.27"). goinstall
          # compiles the linter with go 1.27 so its type-checker matches.
          install-mode: goinstall
          working-directory: ./backend
          args: --timeout=5m

  test-and-security:
    name: Test & Security
    needs: quality-gate
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: './backend/go.mod'
          cache-dependency-path: backend/go.sum
          cache: true

      - name: Run Tests (includes build)
        run: |
          go test -v -race -coverprofile=coverage.out -covermode=atomic ./...

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          file: ./backend/coverage.out
          flags: backend
        continue-on-error: true

      - name: Run Gosec
        # Pinned to a release tag, not @master: a branch ref changes with no
        # signal and Dependabot cannot track it. Now managed by the
        # github-actions ecosystem in .github/dependabot.yml.
        uses: securego/gosec@v2.29.0
        with:
          args: -no-fail -fmt sarif -out results.sarif ./...
